# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Node / frontend
node_modules/
frontend/dist/
*.local

# Env / secrets
.env
.env.*
!.env.example

# Datos crudos y artefactos (no versionar RAW pesado)
data/raw/
*.dump
*.sqlite

# OS / editor
.DS_Store
.idea/
.vscode/

# shards temporales de workers paralelos
data/shards/

# CSV monolítico (se versiona comprimido .gz)
data/funcionarios.csv
data/airhsp_por_pliego_regimen.csv.gz
data/salarios_pt_*.csv

# histórico (se versiona comprimido)
data/funcionarios_historico.csv
data/_con_datos.csv
data/historico.checkpoint.json

# RNSSC crudo: contiene DNI completo (PII sensible), no versionar
data/sanciones_rnssc.csv
data/rnssc.checkpoint.json

# Contratos OCDS crudo: el RUC de persona natural (10…) embebe el DNI (PII sensible).
# Los JSON publicados ya enmascaran ese DNI; el CSV crudo se queda local, no se versiona.
# (El checkpoint solo guarda SHAs de meses — ese sí es seguro de versionar.)
data/contratos.csv
data/_*.log
data/dj_intereses.csv
